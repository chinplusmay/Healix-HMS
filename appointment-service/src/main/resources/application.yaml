server:
  port: 8085

spring:
  application:
    name: appointment-service

  # Local dev defaults to the H2 in-memory profile so `mvnw spring-boot:run`
  # works with no external DB. Override with SPRING_PROFILES_ACTIVE=prod
  # (or -Dspring-boot.run.profiles=prod) to use the real MySQL config below.
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:hms_appointment}?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  cloud:
    openfeign:
      client:
        config:
          # Per-client timeouts. Tight by design: appointment creation is interactive,
          # so we'd rather fail fast and let the client retry than tie up a thread.
          patient-service:
            connect-timeout: 2000
            read-timeout: 3000
            logger-level: basic
          doctor-service:
            connect-timeout: 2000
            read-timeout: 3000
            logger-level: basic

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized
